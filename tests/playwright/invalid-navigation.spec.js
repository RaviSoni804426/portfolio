Update the test script to handle the case where the page does not exist, for example by using a try-catch block to catch the error and assert that the correct error page is displayed: const { test, expect } = require('@playwright/test'); test('Invalid Navigation', async ({ page }) => { try { await page.goto('/non-existent-page'); } catch (error) { await expect(page).toHaveTitle('404'); } });