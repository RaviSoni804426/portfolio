Update the test script to handle the case where the page does not exist, for example by checking the page status code after navigation: await page.goto('/non-existent-page', { waitUntil: 'networkidle' }); const statusCode = await page.response().status(); await expect(statusCode).toBe(404);